{
   "build": {
      "env": {
         "PRISMA_GENERATE_SKIP_AUTOINSTALL": "true"
      }
   },
   "installCommand": "pnpm install && pnpm exec prisma generate",
   "buildCommand": "pnpm run build"
}